# Netlify 設定ファイル — 株式会社TEZU Intelligence HP

[build]
  publish = "."
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "18"

# /api/* → Netlifyサーバーレス関数にルーティング
[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
